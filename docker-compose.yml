services:
  price-agent:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./assets:/app/assets
      - ./.env:/app/.env
    shm_size: "1g"
    restart: unless-stopped
